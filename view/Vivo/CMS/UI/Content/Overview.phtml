<div class="Overview">
<? if(count($children)) : ?>
    <ul>
    <? foreach($children as $child) : ?>
        <li><a href="<?=$this->document($child)?>"><?=htmlspecialchars($child->getOverviewTitleSafe());?></a></li>
    <? endforeach; ?>
    </ul>
<? endif; ?>
</div>